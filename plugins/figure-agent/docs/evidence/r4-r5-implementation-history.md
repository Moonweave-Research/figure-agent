# R4/R5 implementation and Fig1 repair history

> **Document status: Evidence — non-authoritative and not agent-executable.**
> Current product direction and remaining work live only in
> [`../figure-agent.md`](../figure-agent.md).

This record preserves the dated implementation detail removed from the compact
authority. It is a history of mechanisms and measurements, not a current-state,
release, golden, or publication verdict.

## R4 implementation evidence

- Attempt state bound identity, append-only lineage, freshness, actors, legal
  transitions, and terminal development outcomes. Canonical resolution stopped
  on invalid, ambiguous, stale, or symlinked state instead of falling through
  to a legacy loop.
- The v4 repair authority bound semantic object/relation references to the same
  exact source selector as the editable target. Missing or ambiguous authority
  produced only a named `human_attributor` handoff.
- Repair-bound execution consumed an explicit response and recomputed preview,
  required named authorization, wrote adjacent materialized source, and
  preserved immutable reviewed source. Strict failure recovered or rolled back;
  strict success stopped at `machine_repaired`.
- Post-repair review used hash-bound full, target, neighbor, print, and later
  seam views. It published a request and stopped before external host review.
- Root, loop, runner, and queue execution used recoverable fixture admission.
  Drift stopped as stale plan; busy and invalid admission remained typed stops.
  Plan-only execution acquired no mutation lease.
- Each executed step recorded sanitized content-hash evidence for allowlisted
  outputs. Capture diagnostics never changed the underlying command result.

## R5 Fig1 bounded-repair lineage

The failure-first Fig1 lineage began from a fresh B render with 35 attributed
visual candidates: 16 exact, 18 ambiguous, and 1 unbound. It was failure
evidence rather than a quality baseline.

| Child | Bounded repair and observed result |
|---|---|
| C1 | Moved the Panel A `sulfur segment` label below the chain. A measured 0.262 cm frame protrusion was removed while preserving the chain. |
| C2 | Split the Panel B increasing-sulfur arrow around its label. Three zero-distance undeclared-geometry crossings fell to zero without page-geometry change. |
| C3 | Repositioned two Panel F labels with measured frame clearance; two ambiguous ground-related labels remained open. |
| C4 | Restacked Panel E `grounded substrate` and nudged the sibling sample label; boundary crossings fell from four to two. |
| C5 | Repositioned three confirmed text-on-path defects and declared six tightly scoped known false positives. Strict blocking fell from ten to zero. |

C5 strict-green established only the machine-valid layer. Named development
acceptance never implied release or publication acceptance. The Panel F ground
return limitation remained an authoring/layout issue rather than a reusable
fixture-coordinate grammar.

R5.3 added a panel-count-agnostic boundary-coverage prototype derived from
ordered panel markers and authored frame rectangles. A replay exposed the Panel
A crossing in B and its removal in C while reporting frameless panels as zero
coverage. Canonical pipeline wiring, transfer to another family, prospective
timing, and cross-family promotion remained open.

## Historical contract families

The lineage exercised attempt state, current-state projection, adjudicated
repair binding, semantic attribution, repair packet/preview/materialization,
post-repair request/receipt/response, root-attempt admission, execution
evidence, legacy quarantine, and named development-verdict contracts. Exact
schema versions are code-level compatibility details and must be read from the
validators and tests rather than this historical record.
