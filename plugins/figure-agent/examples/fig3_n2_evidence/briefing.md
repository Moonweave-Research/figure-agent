# Briefing - fig3_n2_evidence

## 1. What does this figure show? (1-2 sentences)

Multi-evidence convergence pathway showing how three independent observations
(leakage current, ISPD g(E_t), discharge Debye decay) converge on a unified
trap-depth picture for sulfur-rich polymer charge dynamics.

## 2. Domain vocabulary (terms, materials, mechanisms)

- Evidence panels: leakage current (f(t) ∝ t^-n), ISPD inference (g(E_t)
  shallow/deep), discharge (Debye exp(-t/τ_d)).
- Localized traps: chemical origin (electronegativity, polarizability of S),
  physical origin (local potential fluctuations), S-rich segments.
- Convergence: numbered evidence (①②③) → middle polymer-chain motifs →
  right energy band diagram (CB/VB) → terminal g(E_t) outcome.

## 3. Composition intent (panel layout, flow direction)

Three-zone horizontal layout: left = three numbered evidence panels stacked
vertically, middle = polymer-chain rows showing localized traps with origin
annotations, right = band diagram + g(E_t) outcome plot. Convergent flow
(arrows from each evidence panel to the right column).

## 4. Stage 2 stub note

This is a **minimum text-only stub** authored for Phase E.5 Stage 2 drift
gate measurement. Anchor positions are derived directly from
coordinate_hints.yaml OCR centroids (1693x929 px reference, 121 px/cm scale).
Graphics (chains, bands, plots, arrows) intentionally omitted — drift gate
measures text anchor positions only, so graphics fidelity is out of scope
for the generality verification.

If/when fig3_n2_evidence promotes to a full figure (visual refit), this
briefing should expand with composition details, color choices, and stroke
weights matching the polymer-default style.
