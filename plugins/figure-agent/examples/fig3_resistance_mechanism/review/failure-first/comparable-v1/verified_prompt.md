# Verified condition

Create one editable standalone TikZ/LaTeX source named `verified_generated.tex`.

Read only these shared scientific-authority inputs:

- `../../../briefing.md`
- `../../../spec.yaml`
- `../../../authoring_contract.md`
- `../../../panel_goals.md`

Start from `blank_start.txt`. Do not read the maintained source, earlier Fig3
attempts, Fig1 artifacts, review history, detector outputs, or generated images.
Do not use SVG or Python drawing. Use one authoring attempt and output only the
final source file.

Before finalizing, verify in reasoning that the source preserves these declared
relations without prescribing coordinates or paths:

- Panel A is sign-agnostic and shows repeated capture and release during
  conduction, followed by qualitative current decay and resistance increase.
- Panel B evolves from a discrete S60 state to a broad continuous S80
  distribution.
- `n` is horizontal distribution breadth; `rho_60s` is an independent vertical
  magnitude cue. Breadth must not be drawn or labeled as trap density.
- No carrier polarity, trap chemistry, quantitative measured data, or spatial
  trap network is implied.

The semantic verification is the only treatment. Composition and visual style
remain free. This condition is comparison evidence, not publication acceptance.
