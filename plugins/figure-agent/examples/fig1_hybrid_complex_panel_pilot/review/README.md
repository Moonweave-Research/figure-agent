# Hybrid pilot review boundary

`inherited-source-critique.md` preserves the review history of
`fig1_overview_v2_pair_001_vault`. Its crop paths and verdict bind the source
fixture, not this hybrid render.

`machine_gate_receipt.json` records only reproducibility and machine findings.
It does not replace either required human verdict. The live pilot critique and
human scaffold/artifact verdicts are intentionally absent until Task 7 runs the
predeclared comparison and binds the exact review inputs.
