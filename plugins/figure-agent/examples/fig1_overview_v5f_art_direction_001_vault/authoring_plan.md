# Authoring Plan: fig1_overview_v5f_art_direction_001_vault

## Inputs Read

- `briefing.md`
- `design.md`
- `spec.yaml`
- `authoring_contract.md`
- `theory_guard.md`
- `coordinate_hints.yaml`
- `candidate_plan.md`
- authoring context pack from `fig-agent context-pack`

## Non-Negotiable Boundaries

- Do not edit accepted, golden, release, or final-artifact state.
- Do not edit v5d or v5e.
- Do not add new commands, schemas, public APIs, or dependencies.
- Do not change the scientific story: Panel A linear poly(S-r-DIB), Panel C
  shallow/deep trap grammar, Row 2 independent convergent evidence, and Panel F
  Coulomb-only mechanics stay locked.
- Do not solve Panel F by typography alone; redesign its mechanism geometry.

## Patch Order

1. Figure spine and Row 1 hierarchy:
   - increase Panel C's first-glance dominance;
   - keep A and B quieter without losing chemistry/composition legibility.
2. Panel C hero:
   - clean the trap slab and energy-diagram relation;
   - preserve `mobility edge`, `shallow`, `deep`, and Delta-E_t semantics;
   - avoid label-path regressions near the mobility-edge line and escape curves.
3. Row 2 evidence grammar:
   - make D/E/F read as kinetic, ISPD, and mechanical evidence modes;
   - reduce apparatus dominance and elevate result curves/mechanisms.
4. Panel F mechanism:
   - rebuild the charge/electrode/air-gap/force geometry as one compact scene;
   - keep Coulomb-only directionality and the top-clip vertical cantilever
     convention;
   - keep `q_tr` and `trapped charge` print-readable.
5. Detector repair:
   - run compile;
   - fix text-boundary, label-path, visual-clash, physics-grounding, and TeX
     assertion failures introduced by the redesign.
6. Comparison and gate closure:
   - generate v5d/v5e/v5f contact sheets and panel crops;
   - record pixel-diff and human-gate notes;
   - create fresh critique, adjudication, export, and final status evidence.

## Panel-Specific Targets

- Panel C: the visual hero. It should own the most saturated and spatially
  resolved trap story without becoming crowded.
- Panel D: the reader should see the power-law kinetic distinction first, then
  the SMU/MIM apparatus.
- Panel E: the decay and g(E_t) result should read as the ISPD evidence payload;
  the corona/probe apparatus should remain supportive.
- Panel F: the charge-induced mechanical response should be legible as a
  mechanism scene with clear air gap, electrode, trapped charges, and repulsion.

## Human Checkpoints

- Human comparison is required before accepting v5f over v5d/v5e.
- Human acceptance is required before any accepted/golden/release mutation.
- If the 2 percent v5d pixel-diff floor is missed, this pass is too local and
  should be redesigned or discarded rather than promoted.
