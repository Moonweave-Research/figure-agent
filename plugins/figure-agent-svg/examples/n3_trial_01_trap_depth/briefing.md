# n3_trial_01_trap_depth

SVG-first dogfood figure using the prior `codex_gen_v1.png` as the locked
reference image. The source SVG should remain semantic and editable: vtracer is
only coordinate evidence, not final source geometry.

## Figure Intent

Show how discharge-current power-law behavior, mathematical interpretation, and
sulfur-rich polymer structure converge into one trap-depth picture.

## Production Boundary

- Keep `reference/codex_gen_v1.png` as the draft/reference image.
- Keep `underlay/n3_trial_01_trap_depth.underlay.svg` as locked coordinate
  evidence.
- Edit `source/n3_trial_01_trap_depth.svg` as semantic SVG objects.
- Exported PDF/PNG/TIFF artifacts are generated outputs.
