# Causal Reference Binding v16

## Reference Roles

- `reference/source_variant_aesthetic_ref.png` remains the visual scaffold authority for panel bounds, local boxes, object slots, flow anchors, and overall composition.
- The user-provided causal diagram from the v16 planning turn is a causal/semantic reference for Fig1 narrative binding.
- The causal diagram is not ground_truth and is not a pixel-tracing target.
- The causal diagram must not replace `visual_layout.yaml` or the scaffold contract.

## Bound Causal Chain

The causal reference contributes this interpretation chain:

```text
I(t) ~ t^-n -> n -> Debye exp(-t/tau) -> tau_d -> g(Et)
```

The chain is represented in the Fig1 semantic payload, while the existing center-hero scaffold remains the layout authority.

## Payload Binding

- Electrical evidence binds current decay to the extracted exponent `n`.
- Mathematical interpretation binds the Debye reference and delay parameter `tau_d` to `g(Et)`.
- Molecular origin binds S-rich segments to localized traps through chemical origin and physical origin cues.
- The center hero binds the resulting `g(Et)` interpretation to the converged trap-depth picture.

## Authority Boundary

The causal diagram strengthens semantic meaning and figure narrative. It does not validate the physics by itself, does not set numeric ground truth, and does not license pixel-tracing or card-layout changes.
